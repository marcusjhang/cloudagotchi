# ESP-IDF
firmware/build/
firmware/sdkconfig
firmware/sdkconfig.old
firmware/managed_components/
# firmware/dependencies.lock is committed on purpose (pinned component versions)

# Python
.venv/
__pycache__/

# Raw AI sprite output before processing (large, regenerable)
tools/sprites/raw/

# Local tooling
.claude/
.DS_Store
